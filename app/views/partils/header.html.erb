<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Brand</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if @current_user && @current_user.is_admin %>
      <li><a href="<%= admin_dashboard_path %>">Người dùng</a></li>
        <% else %>
      <li><a href="<%= teacher_courses_path %>">Khoá học</a></li>
      <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if @current_user %>
        <li><a><%= @current_user.name %></a></li>
        <li class="active"><a href="<%= logout_path %>">Logout <span class="sr-only">(current)</span></a></li>
        <% else %>
        <li class="active"><a href="<%= login_path %>">Login <span class="sr-only">(current)</span></a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
